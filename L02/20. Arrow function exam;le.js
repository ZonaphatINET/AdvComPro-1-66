const sun = (firstParma, secondParam) => {
    return firstParma + secondParam;
}
console.log(sun(2, 5));

const printHello = () => {
    console.log("Hello");
};
printHello();

const checkWeight = (weight) => {
    console.log(`Baggage weight : ${weight} kilograms.`);
}
checkWeight(25);

const multiply = (a, b) => a * b;
console.log(multiply(2, 30));